---
title: "Mcqueenのblog"
// 引入播放器组件
import MusicPlayer from '../components/MusicPlayer.astro';
---

<!DOCTYPE html>
<html lang="zh-cn">
<head>
  <!-- 你的原有head内容：背景、字体、博客样式等，完全保留 -->
  <meta charset="utf-8" />
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="generator" content={Astro.generator} />
  <title>Mcqueenのblog</title>
  <style>
    /* 只保留你的博客主体样式，删掉播放器样式 */
    html {
      background-color: #f1f5f9;
      font-family: 'Microsoft YaHei', 'PingFang SC', 'Helvetica Neue', 'Arial', sans-serif;
      background-image: url("/images/pic1.png");
      background-repeat: no-repeat;
      background-size: cover;
      background-position: center center;
      background-attachment: fixed;
    }
    body {
      min-height: 100vh;
      font-family: "PingFang SC", "Microsoft YaHei", "微软雅黑", sans-serif;
      overflow: hidden;
    }
    a { text-decoration: none; font-family: inherit; }
    .blog-container {
      position: fixed;
      top: 0;
      right: 0;
      width: 33.333%;
      height: 100vh;
      background-color: rgba(255, 255, 255, 0.85);
      box-shadow: -8px 0 30px rgba(0, 0, 0, 0.1);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
      z-index: 10;
    }
    .blog-card {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 36px;
      width: 100%;
      max-width: 400px;
    }
    .blog-title {
      font-size: 44px;
      font-weight: 800;
      letter-spacing: 6px;
      text-align: center;
      background: linear-gradient(135deg, #4299e1, #63b3ed, #3182ce);
      -webkit-background-clip: text;
      background-clip: text;
      color: transparent;
      text-shadow: 0 2px 4px rgba(66, 153, 225, 0.2), 0 0 1px rgba(0,0,0,0.1);
    }
    .blog-subtitle {
      font-size: 24px;
      color: #607088;
      font-weight: 400;
      letter-spacing: 2px;
      text-align: center;
      margin-top: 4px;
    }
    .blog-btn {
      display: inline-block;
      padding: 20px 60px;
      font-size: 20px;
      color: #fff;
      background-color: #4299e1;
      text-decoration: none;
      border-radius: 10px;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 4px 12px rgba(66, 153, 225, 0.3);
      text-align: center;
      min-width: 260px;
    }
    .blog-btn:hover {
      background-color: #3182ce;
      transform: scale(1.05);
      box-shadow: 0 6px 16px rgba(66, 153, 225, 0.4);
    }
    .blog-btn:active {
      transform: scale(0.98);
      box-shadow: 0 2px 8px rgba(66, 153, 225, 0.2);
      background-color: #2b6cb0;
    }
    .blog-btn-group {
      display: flex;
      flex-direction: column;
      gap: 25px;
      width: 100%;
      align-items: center;
    }
    @media (max-width: 768px) {
      .blog-container { width: 100%; }
      .blog-title { font-size: 36px; letter-spacing: 4px; }
      .blog-subtitle { font-size: 20px; }
      .blog-btn { padding: 16px 40px; font-size: 18px; min-width: 220px; }
    }
  </style>
</head>
<body>
  <!-- 你的博客主体内容，完全保留 -->
  <div class="blog-container">
    <div class="blog-card">
      <h1 class="blog-title">Mcqueenのblog</h1>
      <h2 class="blog-subtitle">首页</h2>
      <div class="blog-btn-group">
        <a href="/about/" class="blog-btn">关于</a>
        <a href="/blog/" class="blog-btn">博客页面列表</a>
      </div>
    </div>
  </div>

  <!-- 引入播放器组件：就这一行！ -->
  <MusicPlayer />
</body>
</html>